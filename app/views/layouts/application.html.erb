<!DOCTYPE html>
<html>
  <head>
    <title>Bookers2</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <script src="https://kit.fontawesome.com/a7e1b7813b.js" crossorigin="anonymous"></script>

    <%= stylesheet_pack_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>
    <header>
      <nav class="navbar navbar-expand-lg navbar-dark bg-dark">
        <div class="container"><a class="navbar-brand" href="/">
            <span>Bookers</span>
          </a>
          <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNavDropdown"
          　　　　　　aria-controls="navbarNavDropdown" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
          </button>
          <div class="collapse navbar-collapse" id="navbarNavDropdown">
            <ul class="navbar-nav ml-auto">
            <% if user_signed_in? %>
            <li class="nav-item">
              <%= link_to 'Home', user_path(current_user.id), class: "fa-solid fa-house-chimney fa-sm nav-link text-light" %>
            </li>
            <li>
              <%= link_to 'Users', users_path(current_user.id), class: "fa-solid fa-users fa-sm nav-link text-light" %>
            </li>
            <li>  
              <%= link_to 'Books', books_path(current_user.id), class: "fa-solid fa-book-open fa-sm nav-link text-light" %>
            </li>
            <li>
              <%= link_to "Log out", destroy_user_session_path, method: :delete, class: "fa-solid fa-right-from-bracket fa-sm nav-link text-light" %>
            </li>
            <% else %>
            <li>
              <%= link_to "Home", root_path, class: "fa-solid fa-house-chimney fa-xs nav-link text-light" %>
            </li>
            <li>
              <%= link_to "About", about_path, class: "fa-solid fa-link fa-xs nav-link text-light" %>
            </li>
            <li>
              <%= link_to "Sign up", new_user_registration_path, class: "fa-solid fa-user-plus fa-xs nav-link text-light" %>
            </li>
            <li>
              <%= link_to "Log in", new_user_session_path, class: "fa-solid fa-right-to-bracket fa-xs nav-link text-light" %>
            </li>
            <% end %>
            </ul>
          </div>
        </div>
      </nav>
    </header>
    <main>
      <% flash.each do |key,value| %>
        <%= content_tag(:div, value, class: "#{key}") %>
      <% end %>
      <%= yield %>
    </main>
    <footer>
      <div class="container mt-5">
        <div class="row">
          <div class="mx-auto">
            <P> CopyRight Infratop.inc</P>
          </div>
        </div>
      </div>
    </footer>
  </body>
</html>
